// 关键特性 / Key Features
// 严格模式（Strict Mode）
// 通过 "use strict"; 启用，强制更严格的语法检查，减少错误。
// Enables stricter syntax checks via "use strict";, reducing errors.

// JSON 支持（JSON Support）
// 新增 JSON.parse() 和 JSON.stringify()，实现数据解析与序列化。
// Adds JSON.parse() and JSON.stringify() for data parsing and serialization.

// 数组方法（Array Methods）
// 引入 forEach(), map(), filter(), reduce(), every(), some() 等功能性方法。
// Functional methods like forEach(), map(), and filter() for array manipulation.

// 对象属性控制（Object Property Control）
// 支持通过 Object.defineProperty() 定义属性特性（如可写性、可枚举性）。
// Defines property attributes (e.g., writable, enumerable) via Object.defineProperty().

// 其他改进（Other Enhancements）
// 包括 Object.create()、Object.keys()，以及字符串 trim() 方法等。
// Includes Object.create(), Object.keys(), and string trim() method.

alert("Some code");
"use strict"; 
alert("Some code");
